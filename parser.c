#include "push_swap.h"

int main(int ac, char **av)
{
    int i;

    if (ac <= 2)
        return (0);

    i = 0;
    while (i < ac)
    {
        t_list *node = malloc(sizeof(t_list));
        if (!node)
            return (0);
        
    }
}

